{"source": "bounds.c", "target": "src/bounds.rs", "map_tag": "file"}
{"source": "node.c", "target": "src/node.rs", "map_tag": "file"}
{"source": "point.c", "target": "src/point.rs", "map_tag": "file"}
{"source": "quadtree.c", "target": "src/quadtree.rs", "map_tag": "file"}
{"source": "quadtree.h", "target": "src/common/quadtree_mod.rs", "map_tag": "file"}
{"source": "quadtree\n├── bounds.c\n├── node.c\n├── point.c\n├── quadtree.c\n└── svf_analysis_output\nH_FILE_LIST\nquadtree.h", "target": "quadtree\n├── Cargo.toml\n└── src\n    ├── bounds.rs\n    ├── common\n    │   ├── mod.rs\n    │   └── quadtree_mod.rs\n    ├── lib.rs\n    ├── node.rs\n    ├── point.rs\n    ├── quadtree.rs\n    └── svf_analysis_output", "map_tag": "project_tree"}
{"trans_rust_code_context": "", "rust_definition_name": "", "map_tag": "code", "Verfi_Tag": "Free_Function", "trans_rust_code": "Free_Function", "source_code_type": "function", "source_c_code_id": ["elision_#quadtree.c#727"], "trans_rust_path": "src/quadtree.rs"}
{"source_c_code_id": ["quadtree_point#quadtree.h#653"], "map_tag": "code", "source_code_type": "struct", "trans_rust_path": "src/common/quadtree_mod.rs", "trans_rust_code_context": "pub struct QuadtreePoint {\n    pub x: f64,\n    pub y: f64,\n}", "trans_rust_code": "#[derive(Debug, Clone, Copy, PartialEq)]\npub struct QuadtreePoint {\n    pub x: f64,\n    pub y: f64,\n}", "rust_definition_name": ["QuadtreePoint"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_bounds#quadtree.h#658"], "map_tag": "code", "source_code_type": "struct", "trans_rust_path": "src/common/quadtree_mod.rs", "trans_rust_code_context": "pub struct QuadtreeBounds<'a> {\n    pub nw: Option<&'a QuadtreePoint>, // Nullable pointer translated to Option<&QuadtreePoint>\n    pub se: Option<&'a QuadtreePoint>, // Nullable pointer translated to Option<&QuadtreePoint>\n    pub width: f64,\n    pub height: f64,\n}", "trans_rust_code": "pub struct QuadtreeBounds<'a> {\n    pub nw: Option<&'a QuadtreePoint>, // Nullable pointer translated to Option<&QuadtreePoint>\n    pub se: Option<&'a QuadtreePoint>, // Nullable pointer translated to Option<&QuadtreePoint>\n    pub width: f64,\n    pub height: f64,\n}", "rust_definition_name": ["QuadtreeBounds"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_point_new#point.c#715"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/point.rs", "trans_rust_code_context": "pub fn quadtree_point_new(x: f64, y: f64) -> Option<Box<QuadtreePoint>>", "trans_rust_code": "pub fn quadtree_point_new(x: f64, y: f64) -> Option<Box<QuadtreePoint>> {\n    let point = Box::new(QuadtreePoint { x, y });\n    Some(point)\n}", "rust_definition_name": ["quadtree_point_new"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_node#quadtree.h#665"], "map_tag": "code", "source_code_type": "struct", "trans_rust_path": "src/common/quadtree_mod.rs", "trans_rust_code_context": "pub struct QuadtreeNode<'a> {\n    pub ne: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub nw: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub se: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub sw: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub bounds: Option<Box<QuadtreeBounds<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeBounds>>\n    pub point: Option<Box<QuadtreePoint>>, // Nullable pointer translated to Option<Box<QuadtreePoint>>\n    pub key: Option<Box<dyn std::any::Any>>, // Nullable void pointer translated to Option<Box<dyn std::any::Any>>\n}", "trans_rust_code": "pub struct QuadtreeNode<'a> {\n    pub ne: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub nw: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub se: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub sw: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub bounds: Option<Box<QuadtreeBounds<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeBounds>>\n    pub point: Option<Box<QuadtreePoint>>, // Nullable pointer translated to Option<Box<QuadtreePoint>>\n    pub key: Option<Box<dyn std::any::Any>>, // Nullable void pointer translated to Option<Box<dyn std::any::Any>>\n}", "rust_definition_name": ["QuadtreeNode"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_bounds_new#bounds.c#730"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/bounds.rs", "trans_rust_code_context": "pub fn quadtree_bounds_new<'a>() -> Option<Box<QuadtreeBounds<'a>>>", "trans_rust_code": "pub fn quadtree_bounds_new<'a>() -> Option<Box<QuadtreeBounds<'a>>> {\n    let bounds = Box::new(QuadtreeBounds {\n        nw: quadtree_point_new(f64::INFINITY, -f64::INFINITY)\n            .map(|point| Box::leak(point) as &'a QuadtreePoint),\n        se: quadtree_point_new(-f64::INFINITY, f64::INFINITY)\n            .map(|point| Box::leak(point) as &'a QuadtreePoint),\n        width: 0.0,\n        height: 0.0,\n    });\n    Some(bounds)\n}", "rust_definition_name": ["quadtree_bounds_new"], "Verfi_Tag": "Fixed_2_Compile_Success"}
{"source_c_code_id": ["quadtree#quadtree.h#675"], "map_tag": "code", "source_code_type": "struct", "trans_rust_path": "src/common/quadtree_mod.rs", "trans_rust_code_context": "pub struct Quadtree<'a> {\n    pub root: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub key_free: Option<Box<dyn FnMut(&mut dyn std::any::Any) + 'a>>, // Nullable function pointer translated to Option<Box<dyn FnMut>>\n    pub length: u32, // Unsigned int translated to u32\n}", "trans_rust_code": "pub struct Quadtree<'a> {\n    pub root: Option<Box<QuadtreeNode<'a>>>, // Nullable pointer translated to Option<Box<QuadtreeNode>>\n    pub key_free: Option<Box<dyn FnMut(&mut dyn std::any::Any) + 'a>>, // Nullable function pointer translated to Option<Box<dyn FnMut>>\n    pub length: u32, // Unsigned int translated to u32\n}", "rust_definition_name": ["Quadtree"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_bounds_extend#bounds.c#715"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/bounds.rs", "trans_rust_code_context": "pub fn quadtree_bounds_extend(\n    bounds: Option<&mut QuadtreeBounds>,\n    x: f64,\n    y: f64,\n) {\n    todo!(\"Implementation ....\");\n}", "trans_rust_code": "pub fn quadtree_bounds_extend(\n    bounds: Option<&mut QuadtreeBounds>,\n    x: f64,\n    y: f64,\n) {\n    todo!(\"Implementation ....\");\n}", "rust_definition_name": ["quadtree_bounds_extend"], "trans_stub_rust_code": "pub fn quadtree_bounds_extend(bounds: Option<&mut QuadtreeBounds>, x: f64, y: f64) {\n    if let Some(bounds) = bounds {\n        if let (Some(nw), Some(se)) = (bounds.nw, bounds.se) {\n            let new_nw = QuadtreePoint {\n                x: f64::min(x, nw.x),\n                y: f64::max(y, nw.y),\n            };\n            let new_se = QuadtreePoint {\n                x: f64::max(x, se.x),\n                y: f64::min(y, se.y),\n            };\n\n            bounds.nw = Some(&new_nw);\n            bounds.se = Some(&new_se);\n        }\n    }\n}", "try_trans_error": "pub fn quadtree_bounds_extend(bounds: Option<&mut QuadtreeBounds>, x: f64, y: f64) {\n    if let Some(bounds) = bounds {\n        if let (Some(nw), Some(se)) = (bounds.nw, bounds.se) {\n            bounds.nw = Some(&QuadtreePoint {\n                x: f64::min(x, nw.x),\n                y: f64::max(y, nw.y),\n            });\n            bounds.se = Some(&QuadtreePoint {\n                x: f64::max(x, se.x),\n                y: f64::min(y, se.y),\n            });\n        }\n    }\n}", "error_log": "error[E0515]: cannot return value referencing local data `bounds.se_storage.0`\n  --> src/bounds.rs:26:5\n   |\n23 |     if let Some(ref se_boxed) = bounds.se_storage {\n   |                 ------------ `bounds.se_storage.0` is borrowed here\n...\n26 |     Some(bounds)\n   |     ^^^^^^^^^^^^ returns a value referencing data owned by the current function\n\nerror[E0505]: cannot move out of `bounds` because it is borrowed\n  --> src/bounds.rs:26:10\n   |\n8  | pub fn quadtree_bounds_new<'a>() -> Option<Box<QuadtreeBounds<'a>>> {\n   |                            -- lifetime `'a` defined here\n...\n23 |     if let Some(ref se_boxed) = bounds.se_storage {\n   |                 ------------ borrow of `bounds.se_storage.0` occurs here\n...\n26 |     Some(bounds)\n   |     -----^^^^^^-\n   |     |    |\n   |     |    move out of `bounds` occurs here\n   |     returning this value requires that `bounds.se_storage.0` is borrowed for `'a`", "Verfi_Tag": "Fixed_5_Compile_Failed"}
{"source_c_code_id": ["quadtree_node_new#node.c#732"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/node.rs", "trans_rust_code_context": "pub fn quadtree_node_new<'a>() -> Option<Box<QuadtreeNode<'a>>>", "trans_rust_code": "pub fn quadtree_node_new<'a>() -> Option<Box<QuadtreeNode<'a>>> {\n    Some(Box::new(QuadtreeNode {\n        ne: None,\n        nw: None,\n        se: None,\n        sw: None,\n        bounds: None,\n        point: None,\n        key: None,\n    }))\n}", "rust_definition_name": ["quadtree_node_new"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_node_isleaf#node.c#723"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/node.rs", "trans_rust_code_context": "pub fn quadtree_node_isleaf(node: Option<&QuadtreeNode>) -> bool", "trans_rust_code": "pub fn quadtree_node_isleaf(node: Option<&QuadtreeNode>) -> bool {\n    match node {\n        Some(n) => n.point.is_some(),\n        None => false,\n    }\n}", "rust_definition_name": ["quadtree_node_isleaf"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"trans_rust_code_context": "", "rust_definition_name": "", "map_tag": "code", "Verfi_Tag": "Free_Function", "trans_rust_code": "Free_Function", "source_code_type": "function", "source_c_code_id": ["quadtree_point_free#point.c#723"], "trans_rust_path": "src/point.rs"}
{"source_c_code_id": ["quadtree_walk#quadtree.c#836"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn quadtree_walk<'a>(\n    root: Option<&'a QuadtreeNode<'a>>,\n    descent: Option<&dyn Fn(&QuadtreeNode<'a>)>,\n    ascent: Option<&dyn Fn(&QuadtreeNode<'a>)>,\n)", "trans_rust_code": "pub fn quadtree_walk<'a>(\n    root: Option<&'a QuadtreeNode<'a>>,\n    descent: Option<&dyn Fn(&QuadtreeNode<'a>)>,\n    ascent: Option<&dyn Fn(&QuadtreeNode<'a>)>,\n) {\n    if let Some(node) = root {\n        if let Some(descent_fn) = descent {\n            descent_fn(node);\n        }\n        if let Some(nw) = node.nw.as_deref() {\n            quadtree_walk(Some(nw), descent, ascent);\n        }\n        if let Some(ne) = node.ne.as_deref() {\n            quadtree_walk(Some(ne), descent, ascent);\n        }\n        if let Some(sw) = node.sw.as_deref() {\n            quadtree_walk(Some(sw), descent, ascent);\n        }\n        if let Some(se) = node.se.as_deref() {\n            quadtree_walk(Some(se), descent, ascent);\n        }\n        if let Some(ascent_fn) = ascent {\n            ascent_fn(node);\n        }\n    }\n}", "rust_definition_name": ["quadtree_walk"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["node_contains_#quadtree.c#723"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn node_contains_(outer: Option<&QuadtreeNode>, it: Option<&QuadtreePoint>) -> bool", "trans_rust_code": "pub fn node_contains_(outer: Option<&QuadtreeNode>, it: Option<&QuadtreePoint>) -> bool {\n    if let (Some(outer_node), Some(it_point)) = (outer, it) {\n        if let Some(bounds) = &outer_node.bounds {\n            if let (Some(nw), Some(se)) = (bounds.nw, bounds.se) {\n                return nw.x < it_point.x && nw.y > it_point.y && se.x > it_point.x && se.y < it_point.y;\n            }\n        }\n    }\n    false\n}", "rust_definition_name": ["node_contains_"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_node_with_bounds#node.c#745"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/node.rs", "trans_rust_code_context": "pub fn quadtree_node_with_bounds<'a>(\n    minx: f64,\n    miny: f64,\n    maxx: f64,\n    maxy: f64,\n) -> Option<Box<QuadtreeNode<'a>>>", "trans_rust_code": "pub fn quadtree_node_with_bounds<'a>(\n    minx: f64,\n    miny: f64,\n    maxx: f64,\n    maxy: f64,\n) -> Option<Box<QuadtreeNode<'a>>> {\n    // Attempt to create a new QuadtreeNode\n    let mut node = quadtree_node_new()?;\n    // Attempt to create new bounds for the node\n    let mut bounds = quadtree_bounds_new()?;\n    // Extend the bounds with the provided coordinates\n    quadtree_bounds_extend(Some(&mut bounds), maxx, maxy);\n    quadtree_bounds_extend(Some(&mut bounds), minx, miny);\n    // Assign the bounds to the node\n    node.bounds = Some(bounds);\n    // Return the constructed node\n    Some(node)\n}", "rust_definition_name": ["quadtree_node_with_bounds"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_node_ispointer#node.c#715"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/node.rs", "trans_rust_code_context": "pub fn quadtree_node_ispointer(node: Option<&QuadtreeNode>) -> bool", "trans_rust_code": "pub fn quadtree_node_ispointer(node: Option<&QuadtreeNode>) -> bool {\n    if let Some(node) = node {\n        node.nw.is_some()\n            && node.ne.is_some()\n            && node.sw.is_some()\n            && node.se.is_some()\n            && !quadtree_node_isleaf(Some(node))\n    } else {\n        false\n    }\n}", "rust_definition_name": ["quadtree_node_ispointer"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_node_isempty#node.c#719"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/node.rs", "trans_rust_code_context": "pub fn quadtree_node_isempty(node: Option<&QuadtreeNode>) -> bool", "trans_rust_code": "pub fn quadtree_node_isempty(node: Option<&QuadtreeNode>) -> bool {\n    match node {\n        Some(n) => {\n            n.nw.is_none()\n                && n.ne.is_none()\n                && n.sw.is_none()\n                && n.se.is_none()\n                && !quadtree_node_isleaf(Some(n))\n        }\n        None => false, // If `node` is `None`, it cannot be empty.\n    }\n}", "rust_definition_name": ["quadtree_node_isempty"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"trans_rust_code_context": "", "rust_definition_name": "", "map_tag": "code", "Verfi_Tag": "Free_Function", "trans_rust_code": "Free_Function", "source_code_type": "function", "source_c_code_id": ["quadtree_bounds_free#bounds.c#724"], "trans_rust_path": "src/bounds.rs"}
{"source_c_code_id": ["quadtree_node_reset#node.c#727"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/node.rs", "trans_rust_code_context": "pub fn quadtree_node_reset<'a>(\n    node: Option<&mut QuadtreeNode<'a>>, \n    key_free: Option<&mut dyn FnMut(&mut dyn std::any::Any)>\n)", "trans_rust_code": "pub fn quadtree_node_reset<'a>(\n    node: Option<&mut QuadtreeNode<'a>>, \n    key_free: Option<&mut dyn FnMut(&mut dyn std::any::Any)>\n) {\n    if let (Some(node), Some(key_free)) = (node, key_free) {\n        if let Some(key) = node.key.as_mut() {\n            key_free(key.as_mut());\n        }\n    }\n}", "rust_definition_name": ["quadtree_node_reset"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["get_quadrant_#quadtree.c#737"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn get_quadrant_<'a>(\n    root: Option<&'a QuadtreeNode<'a>>,\n    point: Option<&'a QuadtreePoint>,\n) -> Option<&'a QuadtreeNode<'a>>", "trans_rust_code": "pub fn get_quadrant_<'a>(\n    root: Option<&'a QuadtreeNode<'a>>,\n    point: Option<&'a QuadtreePoint>,\n) -> Option<&'a QuadtreeNode<'a>> {\n    if let Some(root_node) = root {\n        if node_contains_(root_node.nw.as_deref(), point) {\n            return root_node.nw.as_deref();\n        }\n        if node_contains_(root_node.ne.as_deref(), point) {\n            return root_node.ne.as_deref();\n        }\n        if node_contains_(root_node.sw.as_deref(), point) {\n            return root_node.sw.as_deref();\n        }\n        if node_contains_(root_node.se.as_deref(), point) {\n            return root_node.se.as_deref();\n        }\n    }\n    None\n}", "rust_definition_name": ["get_quadrant_"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_new#quadtree.c#804"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn quadtree_new<'a>(minx: f64, miny: f64, maxx: f64, maxy: f64) -> Option<Box<Quadtree<'a>>>", "trans_rust_code": "pub fn quadtree_new<'a>(minx: f64, miny: f64, maxx: f64, maxy: f64) -> Option<Box<Quadtree<'a>>> {\n    // Attempt to allocate a new Quadtree instance\n    let mut tree = Box::new(Quadtree {\n        root: None,\n        key_free: None,\n        length: 0,\n    });\n    // Initialize the root node with the given bounds\n    tree.root = quadtree_node_with_bounds(minx, miny, maxx, maxy);\n    // If root initialization fails, return None\n    if tree.root.is_none() {\n        return None;\n    }\n    // Return the successfully created Quadtree wrapped in a Box\n    Some(tree)\n}", "rust_definition_name": ["quadtree_new"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["quadtree_node_free#node.c#754"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/node.rs", "trans_rust_code_context": "pub fn quadtree_node_free<'a>(\n    node: Option<&mut QuadtreeNode<'a>>, \n    key_free: Option<&mut dyn FnMut(&mut dyn std::any::Any)>\n)", "trans_rust_code": "pub fn quadtree_node_free<'a>(\n    node: Option<&mut QuadtreeNode<'a>>, \n    key_free: Option<&mut dyn FnMut(&mut dyn std::any::Any)>\n) {\n    quadtree_node_reset(node, key_free);\n}", "rust_definition_name": ["quadtree_node_free"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["reset_node_#quadtree.c#729"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn reset_node_<'a>(\n    tree: Option<&mut Quadtree<'a>>, \n    node: Option<&mut QuadtreeNode<'a>>\n)", "trans_rust_code": "pub fn reset_node_<'a>(\n    tree: Option<&mut Quadtree<'a>>, \n    node: Option<&mut QuadtreeNode<'a>>\n) {\n    if let Some(tree_ref) = tree {\n        if tree_ref.key_free.is_some() {\n            // Placeholder for logic if `key_free` is not None\n        } else {\n            // Placeholder for logic if `key_free` is None\n        }\n    } else {\n        // Placeholder for logic if `tree` is None\n    }\n}", "rust_definition_name": ["reset_node_"], "Verfi_Tag": "No_Fix_Compile_Success"}
{"source_c_code_id": ["find_#quadtree.c#769"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn find_<'a>(\n    node: Option<&'a QuadtreeNode<'a>>, \n    x: f64, \n    y: f64\n) -> Option<&'a QuadtreePoint> {\n    None\n}", "trans_rust_code": "pub fn find_<'a>(\n    node: Option<&'a QuadtreeNode<'a>>, \n    x: f64, \n    y: f64\n) -> Option<&'a QuadtreePoint> {\n    None\n}", "rust_definition_name": ["find_"], "trans_stub_rust_code": "pub fn find_<'a>(\n    node: Option<&'a QuadtreeNode<'a>>,\n    x: f64,\n    y: f64,\n) -> Option<&'a QuadtreePoint> {\n    if let Some(node_ref) = node {\n        if quadtree_node_isleaf(Some(node_ref)) {\n            if let Some(point_ref) = &node_ref.point {\n                if point_ref.x == x && point_ref.y == y {\n                    return Some(point_ref);\n                }\n            }\n        } else {\n            // Create the QuadtreePoint with a proper lifetime before passing it to get_quadrant_\n            let search_point = QuadtreePoint { x, y };\n            return find_(\n                get_quadrant_(\n                    Some(node_ref),\n                    Some(&search_point), // Pass the reference to the variable\n                ),\n                x,\n                y,\n            );\n        }\n    }\n    None\n}", "try_trans_error": "pub fn find_<'a>(\n    node: Option<&'a QuadtreeNode<'a>>,\n    x: f64,\n    y: f64,\n) -> Option<&'a QuadtreePoint> {\n    if let Some(node_ref) = node {\n        if quadtree_node_isleaf(Some(node_ref)) {\n            if let Some(point_ref) = &node_ref.point {\n                if point_ref.x == x && point_ref.y == y {\n                    return Some(point_ref);\n                }\n            }\n        } else {\n            let test = QuadtreePoint { x, y };\n            return find_(\n                get_quadrant_(Some(node_ref), Some(&test)),\n                x,\n                y,\n            );\n        }\n    }\n    None\n}", "error_log": "error[E0515]: cannot return value referencing local variable `search_point`\n   --> src/quadtree.rs:108:20\n    |\n108 |               return find_(\n    |  ____________________^\n109 | |                 get_quadrant_(\n110 | |                     Some(node_ref),\n111 | |                     Some(&search_point), // Pass the reference to the variable\n    | |                          ------------- `search_point` is borrowed here\n...   |\n114 | |                 y,\n115 | |             );\n    | |_____________^ returns a value referencing data owned by the current function", "Verfi_Tag": "Fixed_5_Compile_Failed"}
{"trans_rust_code_context": "", "rust_definition_name": "", "map_tag": "code", "Verfi_Tag": "Free_Function", "trans_rust_code": "Free_Function", "source_code_type": "function", "source_c_code_id": ["quadtree_free#quadtree.c#827"], "trans_rust_path": "src/quadtree.rs"}
{"source_c_code_id": ["insert_#quadtree.c#781", "split_node_#quadtree.c#745"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn insert_<'a>(\n    tree: Option<&'a Quadtree<'a>>, // Immutable access to `tree`\n    root: Option<&'a mut QuadtreeNode<'a>>, // Mutable access to `root`\n    point: Option<&'a QuadtreePoint>, // Nullable pointer to `QuadtreePoint`\n    key: Option<Box<dyn Any>>, // Nullable void pointer translated to Option<Box<dyn Any>>\n) -> i32 {\n    // Default return value\n    0\n}\n\npub fn split_node_<'a>(\n    tree: Option<&'a Quadtree<'a>>, // Nullable pointer to `Quadtree`\n    node: Option<&'a mut QuadtreeNode<'a>>, // Nullable pointer to `QuadtreeNode`\n) -> i32 {\n    // Default return value\n    0\n}", "trans_rust_code": "pub fn insert_<'a>(\n    tree: Option<&'a Quadtree<'a>>, // Immutable access to `tree`\n    root: Option<&'a mut QuadtreeNode<'a>>, // Mutable access to `root`\n    point: Option<&'a QuadtreePoint>, // Nullable pointer to `QuadtreePoint`\n    key: Option<Box<dyn Any>>, // Nullable void pointer translated to Option<Box<dyn Any>>\n) -> i32 {\n    // Default return value\n    0\n}\n\npub fn split_node_<'a>(\n    tree: Option<&'a Quadtree<'a>>, // Nullable pointer to `Quadtree`\n    node: Option<&'a mut QuadtreeNode<'a>>, // Nullable pointer to `QuadtreeNode`\n) -> i32 {\n    // Default return value\n    0\n}", "rust_definition_name": ["insert_", "split_node_"], "trans_stub_rust_code": "pub fn insert_<'a>(\n    tree_key_free: Option<&'a Quadtree<'a>>, // Immutable access to `key_free`\n    mut root: Option<&'a mut QuadtreeNode<'a>>, // Mutable access to `root`\n    mut point: Option<Box<QuadtreePoint>>, // Ownership of `point` is passed\n    mut key: Option<Box<dyn std::any::Any>>,   // Ownership of `key` is passed\n) -> i32 {\n    if let Some(root_node) = root.as_deref_mut() {\n        if quadtree_node_isempty(Some(root_node)) {\n            root_node.point = point.take();\n            root_node.key = key.take();\n            return 1;\n        } else if quadtree_node_isleaf(Some(root_node)) {\n            if let Some(existing_point) = root_node.point.as_ref() {\n                if let Some(new_point) = point.as_ref() {\n                    if existing_point.x == new_point.x && existing_point.y == new_point.y {\n                        if let Some(tree_ref) = tree_key_free {\n                            reset_node_(Some(tree_ref), Some(root_node));\n                        }\n                        root_node.point = point.take();\n                        root_node.key = key.take();\n                        return 0;\n                    } else {\n                        if let Some(tree_ref) = tree_key_free {\n                            if !split_node_(Some(tree_ref), Some(root_node)) {\n                                return 0;\n                            }\n                            return insert_(tree_key_free, root, point, key);\n                        }\n                    }\n                }\n            }\n            return 1;\n        } else if quadtree_node_ispointer(Some(root_node)) {\n            if let Some(new_point) = point.as_ref() {\n                if let Some(quadrant) = get_quadrant_(Some(root_node), Some(new_point)) {\n                    return insert_(tree_key_free, Some(quadrant), point, key);\n                }\n            }\n            return 0;\n        }\n    }\n    0\n}\npub fn split_node_<'a>(\n    tree: Option<&'a Quadtree<'a>>, // Immutable access to `tree`\n    node: Option<&'a mut QuadtreeNode<'a>>, // Mutable access to `node`\n) -> bool {\n    if let Some(node_ref) = node {\n        if let Some(bounds) = node_ref.bounds.as_ref() {\n            if let Some(nw_point) = bounds.nw {\n                let x = nw_point.x;\n                let y = nw_point.y;\n                let hw = bounds.width / 2.0;\n                let hh = bounds.height / 2.0;\n                let nw = quadtree_node_with_bounds(x, y - hh, x + hw, y);\n                let ne = quadtree_node_with_bounds(x + hw, y - hh, x + hw * 2.0, y);\n                let sw = quadtree_node_with_bounds(x, y - hh * 2.0, x + hw, y - hh);\n                let se = quadtree_node_with_bounds(x + hw, y - hh * 2.0, x + hw * 2.0, y - hh);\n                if nw.is_none() || ne.is_none() || sw.is_none() || se.is_none() {\n                    return false;\n                }\n                node_ref.nw = nw;\n                node_ref.ne = ne;\n                node_ref.sw = sw;\n                node_ref.se = se;\n                let old_point = node_ref.point.take();\n                let old_key = node_ref.key.take();\n                if let Some(tree_ref) = tree {\n                    return insert_(Some(tree_ref), Some(node_ref), old_point, old_key) == 1;\n                }\n            }\n        }\n    }\n    false\n}", "try_trans_error": "pub fn insert_<'a>(\n    tree: Option<&'a Quadtree<'a>>,\n    root: Option<&'a mut QuadtreeNode<'a>>,\n    point: Option<Box<QuadtreePoint>>,\n    key: Option<Box<dyn std::any::Any>>,\n) -> i32 {\n    if let Some(root_node) = root {\n        if quadtree_node_isempty(Some(root_node)) {\n            root_node.point = point;\n            root_node.key = key;\n            return 1;\n        } else if quadtree_node_isleaf(Some(root_node)) {\n            if let Some(existing_point) = &root_node.point {\n                if let Some(new_point) = &point {\n                    if existing_point.x == new_point.x && existing_point.y == new_point.y {\n                        if let Some(tree_ref) = tree {\n                            reset_node_(Some(tree_ref), Some(root_node));\n                        }\n                        root_node.point = point;\n                        root_node.key = key;\n                        return 0;\n                    } else {\n                        if let Some(tree_ref) = tree {\n                            if !split_node_(Some(tree_ref), Some(root_node)) {\n                                return 0;\n                            }\n                            return insert_(tree, Some(root_node), point, key);\n                        }\n                    }\n                }\n            }\n            return 1;\n        } else if quadtree_node_ispointer(Some(root_node)) {\n            if let Some(new_point) = &point {\n                if let Some(quadrant) = get_quadrant_(Some(root_node), Some(new_point)) {\n                    return insert_(tree, Some(quadrant), point, key);\n                }\n            }\n            return 0;\n        }\n    }\n    0\n}\npub fn split_node_<'a>(\n    tree: Option<&'a Quadtree<'a>>,\n    node: Option<&'a mut QuadtreeNode<'a>>,\n) -> i32 {\n    if let Some(node_ref) = node {\n        if let Some(bounds) = &node_ref.bounds {\n            if let Some(nw_point) = bounds.nw {\n                let x = nw_point.x;\n                let y = nw_point.y;\n                let hw = bounds.width / 2.0;\n                let hh = bounds.height / 2.0;\n                let nw = quadtree_node_with_bounds(x, y - hh, x + hw, y);\n                let ne = quadtree_node_with_bounds(x + hw, y - hh, x + hw * 2.0, y);\n                let sw = quadtree_node_with_bounds(x, y - hh * 2.0, x + hw, y - hh);\n                let se = quadtree_node_with_bounds(x + hw, y - hh * 2.0, x + hw * 2.0, y - hh);\n                if nw.is_none() || ne.is_none() || sw.is_none() || se.is_none() {\n                    return 0;\n                }\n                node_ref.nw = nw;\n                node_ref.ne = ne;\n                node_ref.sw = sw;\n                node_ref.se = se;\n                let old_point = node_ref.point.take();\n                let old_key = node_ref.key.take();\n                if let Some(tree_ref) = tree {\n                    return insert_(Some(tree_ref), Some(node_ref), old_point, old_key);\n                }\n            }\n        }\n    }\n    0\n}", "error_log": "error[E0597]: `root` does not live long enough\n   --> src/quadtree.rs:113:30\n    |\n107 | pub fn insert_<'a>(\n    |                -- lifetime `'a` defined here\n108 |     tree_key_free: Option<&'a Quadtree<'a>>, // Immutable access to `key_free`\n109 |     mut root: Option<&'a mut QuadtreeNode<'a>>, // Mutable access to `root`\n    |     -------- binding `root` declared here\n...\n113 |     if let Some(root_node) = root.as_deref_mut() {\n    |                              ^^^^---------------\n    |                              |\n    |                              borrowed value does not live long enough\n    |                              argument requires that `root` is borrowed for `'a`\n...\n149 | }\n    |  - `root` dropped here while still borrowed\n\nerror[E0506]: cannot assign to `root_node.point` because it is borrowed\n   --> src/quadtree.rs:125:25\n    |\n107 | pub fn insert_<'a>(\n    |                -- lifetime `'a` defined here\n...\n113 |     if let Some(root_node) = root.as_deref_mut() {\n    |                              ------------------- argument requires that `*root_node` is borrowed for `'a`\n...\n123 |                             reset_node_(Some(tree_ref), Some(root_node));\n    |                                                              --------- `root_node.point` is borrowed here\n124 |                         }\n125 |                         root_node.point = point.take();\n    |                         ^^^^^^^^^^^^^^^ `root_node.point` is assigned to here but it was already borrowed\n\nerror[E0505]: cannot move out of `root` because it is borrowed\n   --> src/quadtree.rs:133:59\n    |\n107 | pub fn insert_<'a>(\n    |                -- lifetime `'a` defined here\n108 |     tree_key_free: Option<&'a Quadtree<'a>>, // Immutable access to `key_free`\n109 |     mut root: Option<&'a mut QuadtreeNode<'a>>, // Mutable access to `root`\n    |     -------- binding `root` declared here\n...\n113 |     if let Some(root_node) = root.as_deref_mut() {\n    |                              -------------------\n    |                              |\n    |                              borrow of `root` occurs here\n    |                              argument requires that `root` is borrowed for `'a`\n...\n133 |                             return insert_(tree_key_free, root, point, key);\n    |                                                           ^^^^ move out of `root` occurs here\n\nerror[E0597]: `point` does not live long enough\n   --> src/quadtree.rs:140:38\n    |\n107 | pub fn insert_<'a>(\n    |                -- lifetime `'a` defined here\n...\n110 |     mut point: Option<Box<QuadtreePoint>>, // Ownership of `point` is passed\n    |     --------- binding `point` declared here\n...\n113 |     if let Some(root_node) = root.as_deref_mut() {\n    |                              ------------------- argument requires that `point` is borrowed for `'a`\n...\n140 |             if let Some(new_point) = point.as_ref() {\n    |                                      ^^^^^ borrowed value does not live long enough\n...\n149 | }\n    | - `point` dropped here while still borrowed\n\nerror[E0505]: cannot move out of `point` because it is borrowed\n   --> src/quadtree.rs:142:67\n    |\n107 | pub fn insert_<'a>(\n    |                -- lifetime `'a` defined here\n...\n110 |     mut point: Option<Box<QuadtreePoint>>, // Ownership of `point` is passed\n    |     --------- binding `point` declared here\n...\n113 |     if let Some(root_node) = root.as_deref_mut() {\n    |                              ------------------- argument requires that `point` is borrowed for `'a`\n...\n140 |             if let Some(new_point) = point.as_ref() {\n    |                                      ----- borrow of `point` occurs here\n141 |                 if let Some(quadrant) = get_quadrant_(Some(root_node), Some(new_point)) {\n142 |                     return insert_(tree_key_free, Some(quadrant), point, key);\n    |                                                                   ^^^^^ move out of `point` occurs here\n    |\n", "Verfi_Tag": "Fixed_5_Compile_Failed"}
{"source_c_code_id": ["quadtree_search#quadtree.c#823"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn quadtree_search<'a>(tree: Option<&'a Quadtree<'a>>, x: f64, y: f64) -> Option<&'a QuadtreePoint>", "trans_rust_code": "pub fn quadtree_search<'a>(tree: Option<&'a Quadtree<'a>>, x: f64, y: f64) -> Option<&'a QuadtreePoint> {\n    tree.and_then(|t| find_(t.root.as_deref(), x, y))\n}", "rust_definition_name": ["quadtree_search"], "Verfi_Tag": "Fixed_1_Compile_Success"}
{"source_c_code_id": ["quadtree_insert#quadtree.c#814"], "map_tag": "code", "source_code_type": "function", "trans_rust_path": "src/quadtree.rs", "trans_rust_code_context": "pub fn quadtree_insert(\n    tree: Option<&mut Quadtree>, // Nullable pointer to `Quadtree`\n    x: f64,                     // Double translated to f64\n    y: f64,                     // Double translated to f64\n    key: Option<Box<dyn Any>>,  // Nullable void pointer translated to Option<Box<dyn Any>>\n) -> i32 {\n    // Default return value for i32\n    0\n}", "trans_rust_code": "pub fn quadtree_insert(\n    tree: Option<&mut Quadtree>, // Nullable pointer to `Quadtree`\n    x: f64,                     // Double translated to f64\n    y: f64,                     // Double translated to f64\n    key: Option<Box<dyn Any>>,  // Nullable void pointer translated to Option<Box<dyn Any>>\n) -> i32 {\n    // Default return value for i32\n    0\n}", "rust_definition_name": ["quadtree_insert"], "trans_stub_rust_code": "pub fn quadtree_insert<'a>(\n    mut tree: Option<&'a mut Quadtree<'a>>, // Declare `tree` as mutable\n    x: f64,\n    y: f64,\n    key: Option<Box<dyn std::any::Any>>, // Nullable pointer to `key`\n) -> i32 {\n    // Create a new QuadtreePoint\n    let point = quadtree_point_new(x, y);\n    if point.is_none() {\n        return 0; // Return 0 if point creation fails\n    }\n\n    // Extract immutable reference to `tree` for `key_free` access\n    let tree_ref = tree.as_ref().map(|t| &**t);\n\n    // Extract mutable reference to `root` for insertion\n    let root_mut = tree.as_mut().and_then(|t| t.root.as_deref_mut());\n\n    // Check if the root node contains the point\n    let root = tree_ref.and_then(|t| t.root.as_deref());\n    if !node_contains_(root, point.as_deref()) {\n        return 0; // Return 0 if the point is not contained in the root node\n    }\n\n    // Attempt to insert the point into the tree\n    if insert_(tree_ref, root_mut, point.as_deref(), key) == 0 {\n        return 0; // Return 0 if insertion fails\n    }\n\n    // Increment the tree's length\n    if let Some(tree) = tree.as_mut() {\n        tree.length += 1;\n    }\n\n    1 // Return 1 on successful insertion\n}", "try_trans_error": "pub fn quadtree_insert<'a>(\n    tree: Option<&mut Quadtree<'a>>, // Nullable pointer to `Quadtree`\n    x: f64,\n    y: f64,\n    key: Option<Box<dyn std::any::Any>>, // Nullable pointer to `key`\n) -> i32 {\n    // Create a new QuadtreePoint\n    let point = quadtree_point_new(x, y);\n    if point.is_none() {\n        return 0; // Return 0 if point creation fails\n    }\n    // Check if the root node contains the point\n    let root = tree.and_then(|t| t.root.as_deref());\n    if !node_contains_(root, point.as_deref()) {\n        return 0; // Return 0 if the point is not contained in the root node\n    }\n    // Attempt to insert the point into the tree\n    let tree_ref = tree.as_ref();\n    let root_mut = tree.and_then(|t| t.root.as_deref_mut());\n    if insert_(tree_ref, root_mut, point.as_deref(), key) == 0 {\n        return 0; // Return 0 if insertion fails\n    }\n    // Increment the tree's length\n    if let Some(tree) = tree {\n        tree.length += 1;\n    }\n    1 // Return 1 on successful insertion\n}", "error_log": "error[E0597]: `point` does not live long enough\n   --> src/quadtree.rs:136:21\n    |\n124 | pub fn quadtree_insert<'a>(\n    |                        -- lifetime `'a` defined here\n...\n131 |     let point = quadtree_point_new(x, y);\n    |         ----- binding `point` declared here\n...\n136 |     let point_ref = point.as_deref();\n    |                     ^^^^^ borrowed value does not live long enough\n...\n140 |     let root_mut = tree.as_mut().and_then(|t| t.root.as_deref_mut());\n    |                                               --------------------- returning this value requires that `point` is borrowed for `'a`\n...\n163 | }\n    | - `point` dropped here while still borrowed\n\nerror[E0597]: `tree` does not live long enough\n   --> src/quadtree.rs:140:20\n    |\n124 | pub fn quadtree_insert<'a>(\n    |                        -- lifetime `'a` defined here\n125 |     mut tree: Option<&'a mut Quadtree<'a>>, // Declare `tree` as mutable\n    |     -------- binding `tree` declared here\n...\n140 |     let root_mut = tree.as_mut().and_then(|t| t.root.as_deref_mut());\n    |                    ^^^^                       --------------------- returning this value requires that `tree` is borrowed for `'a`\n    |                    |\n    |                    borrowed value does not live long enough\n...\n163 | }\n    |  - `tree` dropped here while still borrowed\n\nerror[E0502]: cannot borrow `tree` as immutable because it is also borrowed as mutable\n   --> src/quadtree.rs:143:9\n    |\n124 | pub fn quadtree_insert<'a>(\n    |                        -- lifetime `'a` defined here\n...\n140 |     let root_mut = tree.as_mut().and_then(|t| t.root.as_deref_mut());\n    |                    ----                       --------------------- returning this value requires that `tree` is borrowed for `'a`\n    |                    |\n    |                    mutable borrow occurs here\n...\n143 |         tree.as_ref().and_then(|t| t.root.as_deref()), // Immutable borrow for `node_contains_`\n    |         ^^^^ immutable borrow occurs here\n\nerror[E0502]: cannot borrow `tree` as immutable because it is also borrowed as mutable\n   --> src/quadtree.rs:150:9\n    |\n124 | pub fn quadtree_insert<'a>(\n    |                        -- lifetime `'a` defined here\n...\n140 |     let root_mut = tree.as_mut().and_then(|t| t.root.as_deref_mut());\n    |                    ----                       --------------------- returning this value requires that `tree` is borrowed for `'a`\n    |                    |\n    |                    mutable borrow occurs here\n...\n150 |         tree.as_ref().map(|v| &**v), // Immutable borrow for `insert_`\n    |         ^^^^ immutable borrow occurs here\n\nerror[E0499]: cannot borrow `tree` as mutable more than once at a time\n   --> src/quadtree.rs:159:25\n    |\n124 | pub fn quadtree_insert<'a>(\n    |                        -- lifetime `'a` defined here\n...\n140 |     let root_mut = tree.as_mut().and_then(|t| t.root.as_deref_mut());\n    |                    ----                       --------------------- returning this value requires that `tree` is borrowed for `'a`\n    |                    |\n    |                    first mutable borrow occurs here\n...\n159 |     if let Some(tree) = tree.as_mut() {\n    |                         ^^^^ second mutable borrow occurs here", "Verfi_Tag": "Fixed_5_Compile_Failed"}
